---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: otel-demo
spec:
  instances: 1
  bootstrap:
    initdb:
      database: otel
      postInitApplicationSQLRefs:
        configMapRefs:
          - name: postgresql-init
            key: init.sql
  storage:
    size: 1Gi
